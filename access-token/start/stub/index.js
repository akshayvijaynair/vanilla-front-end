// Define the event handler named generateAccessToken which generates an access token using username, password and timestamp
/* 
Function name should be generateAccessToken
along with showing the alert message, return the same value using return statement in both missing and access token generated scenarios
*/


if (typeof exports !== "undefined") {
  module.exports = {
    generateAccessToken,
  };
}
